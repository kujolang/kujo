# Secret values redact automatically at output boundaries.

api_key := secret("sk-local-demo")

print(api_key)
print(to_json({"api_key": api_key, "label": "demo"}))
print(type(api_key))
print(is_secret(api_key))
print(reveal(api_key) == "sk-local-demo")
