# Owned knowledge

Kujo Agent Projects keep agent definitions, instructions, provider preferences,
project skills, tool declarations, knowledge, policies, workflow declarations,
evaluation contracts, and capability expectations in the repository. The Kujo
CLI discovers and validates the project contract. Kennel owns pinned dependency
installation, AI SDK owns provider calls, Agents SDK owns execution semantics,
RAG owns retrieval, and Eval owns evaluation execution.
