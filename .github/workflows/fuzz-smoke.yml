name: Fuzz Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: '25 7 * * *'

jobs:
  fuzz-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09

      - name: Install Rust toolchains
        run: |
          rustup toolchain install stable --profile minimal
          rustup toolchain install "$(cat fuzz/RUST_TOOLCHAIN)" --profile minimal

      - name: Install cargo-fuzz
        run: cargo +stable install cargo-fuzz --version "$(cat fuzz/CARGO_FUZZ_VERSION)" --locked

      - name: Fuzz pinned targets (smoke)
        run: bash scripts/fuzz_smoke.sh --max-total-time 20 lexer parser xml_bounded
